[tool:pytest]
testpaths = tests
python_files = test_*.py
python_functions = test_*
addopts =
    --tb=short
    --strict-markers
    -v
asyncio_mode = auto
markers =
    asyncio: marks tests as asyncio
    playwright: marks tests as playwright UI tests